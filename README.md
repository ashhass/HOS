# HOS (Hand Object Segmentation)

**Description**

This project aims to automatically generate segments for hands and interacting objects. It uses an HRNet network with ResNet50 backbone pretrained on ImageNet. 
